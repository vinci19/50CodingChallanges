// Find the maximum number in an array of numbers

const numbers = [0, 1, 5, 6, 8, 11, 15]
let max = numbers[0];

for(let i = 0; i < numbers.length; i++ ){
   if(numbers[i] > max){
     max = numbers[i];
   }
}

console.log(max);
